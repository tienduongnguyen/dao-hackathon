import { StyleSheet } from 'react-native';

import { ColorDefault } from '@src/themes/color';

export const styles = StyleSheet.create({
  inputContainer: {
    height: 32,
    width: '60%',
    borderColor: ColorDefault.nav_border_color,
  },
});
